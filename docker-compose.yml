version: '3.8'

services:
  ielts-bot:
    build: .
    container_name: ielts-buddy-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - TZ=Asia/Ho_Chi_Minh
